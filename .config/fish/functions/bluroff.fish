function bluroff --wraps='hyprctl keyword decoration:blur:enabled false' --description 'alias bluroff hyprctl keyword decoration:blur:enabled false'
  hyprctl keyword decoration:blur:enabled false $argv
        
end
